/** Initializes a new Zelda game and starts running it. */
class Main {
    function void main() {
        var ZeldaGame game;
        let game = ZeldaGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}